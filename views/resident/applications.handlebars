{{> residentTemplate}}

<main id="main" class="main">

    <div class="pagetitle">
        <h1>My Applications</h1>
        <nav>
            <ol class="breadcrumb">
                <li class="breadcrumb-item"><a href="/resident/dashboard">Dashboard</a></li>
                <li class="breadcrumb-item active">My Applications</li>
            </ol>
        </nav>
    </div><!-- End Page Title -->

    <section class="section profile">
        <div class="row">
            <div class="col-xl-4">
                <div class="card">
                    <div class="card-body profile-card pt-4 d-flex flex-column align-items-center">
                        <img src="/assets/img/avatar.png" alt="Profile" class="rounded-circle">
                        <h2 class="text-capitalize">{{user.firstName}} {{user.lastName}}</h2>
                        <h2 class="text-capitalize mb-3">#{{user.residentID}}</h2>
                        <h3>KDOC Resident</h3>
                    </div>
                </div>
            </div>

            <div class="col-xl-8">

                <div class="card">
                    <div class="card-body pt-3">
                        <!-- Bordered Tabs -->
                        <ul class="nav nav-tabs nav-tabs-bordered">
                            <li class="nav-item">
                                <button class="nav-link active" data-bs-toggle="tab"
                                    data-bs-target="#profile-overview">Completed Applications</button>
                            </li>
                        </ul>
                        <div class="tab-content pt-2">

                            <div class="tab-pane fade show active profile-overview" id="profile-overview">
                                {{#if appliedJobs}}
                                <div class="row mt-3">
                                    <div class="col-lg-4 col-md-4 label text-capitalize text-muted">Company

                                    </div>
                                    <div class="col-lg-8 col-md-8 text-capitalize text-muted">Position</div>
                                </div>
                                {{#each appliedJobs}}
                                <div class="row mt-3">
                                    <div class="col-lg-4 col-md-4 label text-capitalize">{{this.companyName}}

                                    </div>
                                    <div class="col-lg-8 col-md-8 text-capitalize">{{this.job.position}}</div>
                                </div>
                                <hr>
                                {{/each}}
                                {{else}}
                                <span class="text-warning">You have not submitted any applications yet.
                                </span>
                                {{/if}}
                            </div>
                        </div><!-- End Bordered Tabs -->
                    </div>
                </div>
            </div>
        </div>
    </section>

</main><!-- End #main -->

<!-- ======= Footer ======= -->
{{> footer}}
{{> arrowUp}}