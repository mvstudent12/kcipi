{{> residentTemplate}}

<main id="main" class="main">

    <div class="pagetitle">
        <h1 class="text-capitalize">Recent Activities</h1>
        <nav>
            <ol class="breadcrumb">
                <li class="breadcrumb-item"><a href="resident/dashboard">Dashboard</a></li>
                <li class="breadcrumb-item active text-capitalize">Recent Activities</li>
            </ol>
        </nav>
    </div>

    <section class="section profile">
        <div class="row">


            <div class="col-12">

                <div class="card">

                    <div class="card-body">
                        <h5 class="card-title text-capitalize">{{user.firstName}}
                            {{user.lastName}} <span class="text-capitalize">| KDOC Resident</span>
                        </h5>

                        <div class="activity">
                            <div class="card">
                                <div class="card-body mt-3">
                                    {{#if activities}}
                                        {{#each activities}}
                                            <div class="activity-item d-flex">
                                                <div class="activity-label">{{formatDate this.timestamp}}</div>
                                                <i
                                                    class='bi bi-circle-fill activity-badge {{this.color}} align-self-start'></i>
                                                <div class="activity-content">
                                                    {{this.details}}
                                                </div>
                                            </div>

                                        {{/each}}
                                    {{else}}
                                        <h5 class="text-center pt-3">You have not yet completed any activities.</h5>
                                    {{/if}}
                                </div>
                            </div>
                        </div>

                    </div>
                </div>
            </div>
        </div>
        </div>
        </div>
    </section>
</main>


{{> footer}}
{{> arrowUp}}